const User = require('../models/user.model')
const bcrypt = require('bcrypt')
const jwt = require('jsonwebtoken')
require('dotenv').config()

module.exports = {
    /* These function names match the ones in User Router
    register:
    login:
    logout:
    getOneUser:
    updateOneUser:
    */
}      